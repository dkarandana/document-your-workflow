/**
 * @namespace core
 * @description The parent namespace for the following modules:
 * - {@link Storage} Storing [localstorage](@link https://www.w3.org/TR/webstorage/#the-localstorage-attribute)  data under the core.storage namespace.
 */

import storage from './storage';

export default {
    storage
};